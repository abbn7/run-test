# Optional — raises GitHub API rate limit from 60/hr to 5000/hr
# Create a fine-grained read-only token at https://github.com/settings/tokens
GITHUB_TOKEN=

# Optional — Resend API key for the /contact form (server fn falls back to mailto if unset)
RESEND_API_KEY=
